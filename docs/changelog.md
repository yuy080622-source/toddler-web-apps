# PRJ-003 変更履歴

## 2026-08-29 — APP-004 iPhone実機確認PASS・正式公開

- APP-004「音あそびピアノ」のiPhone実機確認全項目PASSを記録し、PM判断により正式公開済みへ変更
- 複数指、指滑り、同一鍵盤内の非再発音、5音同時発音、高速連打、音OFF保持、中断・復帰、回転、safe area、reduced-motion、ホーム長押し、継続利用、発熱・遅延をPASS
- 共通ポータルの既存データ配列へAPP-004カードをID順で追加
- APP-004の鍵盤数、音色、音量、特殊演出、共通ホーム導線、共通Clarity実装は変更なし
- ポータル表示対象をAPP-002／003／004／006の4本へ更新

## 2026-08-25 — APP-004 公開前検証と公開候補化

### 維持した仕様

- ド〜シの7鍵盤、triangle波、複数pointer、指滑り、同時発音最大5音を維持
- 音OFF時の視覚反応、音OFF設定保存、7回ごとの虹または星の演出を維持
- 回数は内部の演出判定だけに使用し、子ども画面へ表示しない

### 安全性・共通機能

- `visibilitychange`、`pagehide`、`pageshow`、BFCache復帰時の後片付けを追加
- 非表示・途中退出時に発音中ノード、pointer状態、鍵盤タイマー、装飾DOMを停止・削除
- 高速連打時の装飾粒を最大60個に制限
- 約2秒長押しの共通ホーム導線を追加
- PRJ-003共通Clarity Project ID `y7wygqymd5`を共通ファイル経由で追加
- カスタムイベント、カスタムユーザーIDは追加していない

### 検証と判定

- 7鍵盤、Enter／Space、指滑り、同一鍵盤内の非再発音、100回高速操作をPASS
- 音OFF時の視覚反応、再読み込み後のOFF維持、履歴復帰後の一時状態解放をPASS
- 390×844、844×390、1024×768で縦横スクロールなし
- JavaScript構文、差分形式、相対パス、コンソールエラー・警告なしを確認
- 実装commit `c9a0dd7a447ef91dbc68e8f9ac91c0ebf944978e`のGitHub Pagesデプロイrun #24がsuccess
- 公開URLで7鍵盤、通常操作、共通ホーム導線、Clarity Project ID、相対パス、スクロールなし、コンソールエラー・警告なしを確認
- コード検証はPASS。正式公開判定はiPhone実機確認後に行う
- 正式公開前のため共通ポータルカードには未追加

## 2026-08-25 — Microsoft Clarity導入

### 変更

- Project ID `y7wygqymd5`を使用する`shared/clarity.js`を追加
- 共通ポータルとAPP-002／003／006から相対パスで共通ファイルを読み込む構成を追加
- PRJ-003全体を1つのClarity Projectで運用し、URLパス別に各APPを分析する方針を記録
- 初期分析はセッション録画、ヒートマップ、URLパス別分析に限定し、カスタムイベントは追加しない
- カスタムユーザーID、個人情報入力、ログイン、フォーム、広告SDK、他の分析SDKは追加しない
- 将来正式公開されたAPPも同じProjectへ追加する方針を記録

### 安全性

- Clarity本体を非同期で読み込み、読み込み失敗時もポータルと各アプリの本体処理が独立して動作する構成を維持
- アプリ独自の利用統計は端末内保存を基本とし、外部分析はPM承認済みのPRJ-003用Clarityに限定

## 2026-08-25 — 共通ポータル試作版と長押しホーム導線

### 変更

- Repositoryルートへ、APP-002、APP-003、APP-006を選べる共通ポータル試作版を追加
- データ配列から大きな正方形カードを生成し、正式アイコンへ差し替えやすく、アプリ追加時も同じ構造を維持できるようにした
- スマートフォン縦2列、幅広画面3列、タブレット最大4列のレスポンシブ表示、safe area、`100dvh`、標準縦スクロール、reduced-motionへ対応
- 3アプリの上部へ共通ホームボタンを追加し、約2秒長押し完了時だけRepositoryルートへ戻るようにした
- 長押し途中の解除、領域外移動、`pointercancel`、`pointerleave`、`lostpointercapture`では遷移を取り消し、ホーム操作を各アプリの遊びへ伝播させない

### 検証と位置付け

- 390×844、844×390、1024×768でカード3件、列数、横スクロールなしを確認
- 3カードのリンク先、3アプリのホーム短押し非遷移、背景遊びの非発火、長押し取消と2000ms完了を確認
- JavaScript構文、差分形式、ローカル表示のコンソールエラー・警告なしを確認
- 今回は最終デザインではなく実機操作感確認用の試作版。カードサイズ、列数、スクロール感は実機確認後に再判断する

## 2026-08-25 — APP-003 正式音声MP3へ切り替え

### 経緯と判断

- iPhone Safariで、MP3失敗後のWeb Speech APIフォールバック方式と、タップ内で直接読み上げる方式の両方が無音だった
- APP-003ではWeb Speech APIをMVP音声手段として不採用とし、正式MP3をHTML Audio APIで再生する方式へ変更

### 変更

- 4果物の正式MP3を英字名で`apps/APP-003-fruit-pop/assets/sounds/`へ配置
- 果物タップのユーザー操作内で対応する`Audio.play()`を即時呼び出す
- Web Speech API、失敗後フォールバック、不要な404経路を削除
- 720msの自動切り替えでは音声を止めず、再タップ、次の果物タップ、音OFF、非表示・退出時に停止
- 音声失敗時も視覚演出と循環を継続

### 次の確認

- 実装commit `77d7b6993fda02266fe785cff01c3f80c55dafa6`のGitHub PagesデプロイはPASS
- 公開4MP3はHTTP 200で提供元とSHA-256一致
- 公開画面のタップ演出、720ms自動循環、390×844のスクロール、コンソールをPASS
- iPhone Safariで最初に4果物すべての音が出るか確認
- 音が出た場合、語尾、声質、音量を改めて判定
- 正式公開判定は実機音声確認まで保留

## 2026-08-24 — APP-003 iPhone Safari音声FAILと直接読み上げ修正

### 実機結果

- 果物自動循環、高速連打、2本指、縦横回転、safe areaはiPhone実機PASS
- 音声ONでも果物名が出ず、音声は実機FAIL
- 以前のWeb Speech API声質・音量PASS判定があった場合は取り消す
- APP-003の正式公開判定を保留へ戻す

### 原因候補

- 正式MP3未配置でもMP3再生を先に試し、非同期失敗後にWeb Speech APIを呼んでいた
- iPhone Safariでは読み上げ開始がユーザーのタップ操作から離れ、再生が許可されにくい可能性が高い
- 720ms後の自動切り替え時にも音声停止処理が実行されていた

### 修正

- MVPでは存在しないMP3へアクセスせず、果物タップ処理内からWeb Speech APIを直接開始
- 720msの自動切り替えでは読み上げを強制停止しない
- 高速再タップ時は直前の読み上げをキャンセルして即時再開始し、重複を防止
- 音OFF、ページ非表示、途中退出では従来どおり音声停止
- 将来正式MP3を導入するときだけMP3優先へ切り替えられる構造を維持

### Codex回帰テスト

- **PASS** 模擬Web Speech APIでタップ直後に「りんご！」を1回呼び出す
- **PASS** 720ms後の果物切り替えで読み上げキャンセルを追加しない
- **PASS** 5回高速タップ時は直前の読み上げを停止して再開始し、音声を積み重ねない
- **PASS** 音OFF中は読み上げなし、ON復帰後の次タップで読み上げ再開
- **PASS** 途中退出・履歴復帰後の演出・粒子残留なし
- **PASS** 正式MP3未配置状態でMP3アクセスなし
- **PASS** JavaScript構文、差分形式
- **PASS** GitHub Pagesデプロイ（実装commit `5c175d94febd9fb446d308d73a347f4cf3e05ec6`）
- **PASS** 公開JavaScriptに直接読み上げ分岐、MP3無効、自動切り替え時の音声維持を確認
- **PASS** 公開URLで果物演出、自動循環、スクロールなし、コンソールエラー・警告なし
- iPhoneで実際に音が聞こえるかは再確認待ち

### 次の確認

- GitHub Pages反映後、iPhoneでまず果物名が出るかだけ確認する
- 音が出た場合に限り、声質・音量を改めて判定する

## 2026-08-24 — APP-003 果物タップ自動循環への変更

### PM確定仕様

- 子ども画面から「まえ」「つぎ」ボタンとページ表示を削除
- 中央の果物をタップすると、直後に果物固有の視覚演出・名前・音声を返す
- 約720msの演出後、りんご→バナナ→いちご→ぶどうの順で1つだけ自動的に進む
- 演出中の再タップ、複数指、高速タップは同じ果物の演出を再開始し、最後の入力から1回だけ進む
- 子ども画面に回数・スコア・ランキングを表示しない方針を維持

### 維持した既存改善

- 4果物の固有演出、名前、装飾粒、絵文字・Web Speech APIフォールバック
- 音声ON/OFF、音OFF設定保存、高速入力対策
- `visibilitychange`、`pagehide`、`pageshow`、BFCache対応
- safe area、`100dvh`、`prefers-reduced-motion`、キーボード、ARIA
- 古い音声・タイマー・装飾DOMの停止・削除

### Codex回帰テスト

- **PASS** りんご→バナナ→いちご→ぶどう→りんごの循環順
- **PASS** タップ直後の固有演出・名前・通常8粒
- **PASS** 10回高速タップ中も同じ果物・粒子8個・名前表示1個を維持し、最後の入力から約720ms後に1つだけ進む
- **PASS** 演出終了・切り替え・途中退出・履歴復帰後の粒子、名前表示、古い演出クラスの残留なし
- **PASS** 音OFFでの遊び、再読み込み・履歴復帰後の音OFF維持
- **PASS** Enterキー操作
- **PASS** 390×844、844×390、1024×768で縦横スクロールなし
- **PASS** 子ども画面のボタンは果物と音声の2個のみ
- **PASS** JavaScript構文、差分形式、ブラウザコンソールエラー・警告なし
- **PASS** GitHub Pagesのデプロイ完了（実装commit `56ff872ba57b26180d30a73052b59837616a0513`）
- **PASS** 公開URLでナビゲーション削除、タップ直後の8粒、約720ms後のバナナ切り替え、音OFF再読み込み保持、390×844のスクロールなし、コンソールエラー・警告なし

### 実機確認待ち

- iPhone／Android実機の実際の2本指、高速タップ、音量・音質
- safe area、画面回転、バックグラウンド・BFCache復帰
- `prefers-reduced-motion: reduce`有効時の刺激と操作感
- 幼児本人にとって約720msの切り替えが早すぎず遅すぎないか
- 1〜2分利用時の発熱・動作低下

## 2026-08-23 — APP-006 打ち上げ演出の実装・回帰検証

### 変更

- タップ位置で直接開花する方式を、画面下端より少し下から光が打ち上がって到達後に開花する方式へ変更
- タップ直後から小さな光の玉と丸い短い尾をCanvasへ描画
- タップXを維持し、爆発Yを画面高10〜70%へ補正
- 通常の打ち上げ時間を320〜480msに設定
- 打ち上げ光を最大12個に制限し、完了、中断、退出、画面サイズ変更時に破棄
- 追加タイマーや一時DOMを使わず、既存の`requestAnimationFrame`ループへ統合
- 開花時のみ既存効果音を再生し、粒子上限200・同時効果音4・音OFF保存を維持
- `prefers-reduced-motion`時は170〜230ms、横揺れなし、短い尾とし、既存の粒子数・寿命・発光・光跡低減を維持
- APP-006 READMEとPRJ-003 handoffを現行実装へ更新

### 変更理由

- 打ち上げ花火の全体像を分かりやすくしつつ、幼児向けの即時反応を維持するPM確定仕様へ対応するため
- 高速タップや途中退出で打ち上げ光、尾、タイマー、AudioNodeを残留させないため

### Codexテスト

- **PASS** JavaScript構文、差分形式
- **PASS** 390×844、844×390、1024×768でCanvasが表示領域へ一致し、縦横スクロールなし
- **PASS** 上端タップは約10%高、中央はタップY、下端は70%高へ補正
- **PASS** 左右端付近でX位置を維持
- **PASS** タップ直後に打ち上げ光が表示され、到達後に開花
- **PASS** 50回相当の高速・複数位置操作後もDOM37要素・Canvas1個を維持し、コンソールエラー・警告なし
- **PASS** 音ON/OFF、音OFFの再読み込み保持、音声ボタンでの花火誤発火なし
- **PASS** 別ページへの途中退出と履歴復帰後も打ち上げ可能
- **PASS** Enter・Spaceキー操作
- **PASS** 粒子200、打ち上げ光12、同時効果音4、AudioNode切断、中断時破棄、reduced-motion分岐のコード確認
- **PASS** GitHub Pagesで最新JavaScript、CSS／JS相対パス、打ち上げ・開花、音OFF再読み込み保持を確認
- **PASS** 公開URLの390×844表示で横スクロール、コンソールエラー・警告なし
- **FAILなし** Codexで確認できた範囲に公開を妨げる不具合なし

### 実機確認待ち

- iPhone／Android実機での音量・音質
- 実際の複数指操作と高速タップ
- 上部・中央・下部タップ時の見え方とタップ感
- safe area、画面回転、バックグラウンド復帰、BFCache
- `prefers-reduced-motion: reduce`有効時の表示と刺激
- 幼児本人が感じる光量・刺激、長時間利用時の発熱
- After動画撮影とBefore素材との比較

## 2026-08-23 — APP-003 公開前コード検証

### 修正

- `visibilitychange`、`pagehide`、`pageshow`、BFCache復帰処理を追加
- 非表示・途中退出時に演出タイマー、粒子、名前表示、音声を停止・削除
- 古いMP3失敗コールバックが中断後に読み上げを始めないよう、実行ID・音設定・表示状態で制御
- MP3未配置を記録し、以後はWeb Speech APIへ直接フォールバック
- reduced-motion時の粒子数を8個から4個へ減らし、移動距離を短縮
- safe areaに通常余白を加算し、音ボタンを64px以上へ拡大
- 空のfaviconを指定し、不要なfaviconリクエストを抑止

### 検証

- りんご、バナナ、いちご、ぶどうの循環切替と4種類の固有演出を確認
- 画像未配置時に4種類すべて絵文字表示となることを確認
- MP3未配置時に再生失敗を処理し、Web Speech APIフォールバック経路へ入ることを確認
- 高速100回操作後も粒子、名前表示、古い演出DOMが残らないことを確認
- 音OFF時も視覚演出が成立し、再読み込み・BFCache復帰後もOFFが維持されることを確認
- 途中退出・復帰時に演出、粒子、音声状態が残らないことを確認
- 390×844、844×390、1024×768で縦横スクロールなし
- Enterキー、button要素、ARIA、フォーカスを確認
- JavaScript構文、コンソールエラー・警告なし

### 判定・保留

- コード上の公開前検証はPASS。APP-003をスマートフォン実機確認用の公開候補とする
- 子ども画面に回数・スコア表示はない
- 「まえ／つぎ」は果物以外の操作対象・誤操作要因になり得るが、削除・方式変更はせずPM判断待ち
- 実際の複数指、Web Speech APIの日本語音声・音量、safe area、reduced-motion、幼児本人の操作感、長時間利用時の発熱は実機確認待ち

## 2026-08-23 — APP-002 iPhone実機確認・正式公開判定

### 実機確認

以下をiPhone実機で確認し、すべて問題なし。

- 通常操作
- 2本指で別々の泡を同時に割る操作
- 音OFFと、ページを閉じて再度開いた後の音OFF保持
- 縦→横→縦の画面回転
- バックグラウンド移行後の復帰
- 高速連打時の音量・刺激・表示
- 「視差効果を減らす」有効時に泡の動きがゆっくりになること
- 1〜2分程度の継続利用で不自然な発熱・動作低下がないこと

### 判定

- Codex公開前技術検証：PASS
- 回数表示削除のPM判断：反映済み
- iPhone実機確認：PASS
- APP-002「シャボン玉タッチ」をMVP正式公開済みとする
- 公開URL：`https://yuy080622-source.github.io/toddler-web-apps/apps/APP-002-bubble-touch/`
- 公開後は重大不具合がない限り「効果判定待ち」とし、短期間で効果を断定しない

### 発信素材

- 実装完了だけでなく、共通仕様への統一→スマホ実機での複数指・音・回転・復帰・reduced-motion・発熱確認まで行って公開判定した工程は発信価値があるため、共通素材ログへ記録する

## 2026-08-23 — APP-006 打ち上げ演出改善のPM判断

### 背景

- 既存APP-006は、タップした位置で直接花火が開く方式だった
- レビューで「打ち上げ花火として、画面下から上がってから開く方が全体像を理解しやすい」という意見が出た
- 幼児向けの即時反応を損なわず、花火らしさを上げる改善として採用した

### PM判断

- 打ち上げ開始位置は画面下端より少し下
- 地面・街並みは今回追加しない
- タップX座標は基本維持
- タップY座標は基本維持するが、上端・下端だけ安全範囲へ補正
- 上端は画面上端から約10%より下を目安
- 下端は画面高さ約70%より上を最低爆発高度の目安
- 画面下部タップ時もX位置は維持し、Yだけ最低高度まで上げる
- タップ直後に打ち上げ反応を開始し、約0.3〜0.5秒で爆発位置へ到達
- 打ち上げ表現は小さな光の玉＋短い尾
- 上昇音は追加せず、既存花火効果音のみ
- 既存の粒子上限200、同時効果音最大4、音OFF保存、中断・復帰処理を維持
- `prefers-reduced-motion`では打ち上げ時間・移動・尾を短くし、既存の粒子数・寿命・発光低減も維持
- 強い点滅、全画面フラッシュ、突然の大音量は追加しない

### Before記録

- 改善前のiPhone縦画面録画を取得済み
- 約10.8秒
- 旧仕様「タップ位置で直接花火が開く」状態
- Google Drive保存済み
- 素材ログ`MAT-20260823-016`へ紐付け済み
- Afterは新仕様実装後、同程度の長さで上部・中央・下部タップを含めて撮影する

### 状態

- 仕様確定
- GitHub正式資料へ反映済み
- 実装はCodex待ち
- 実装後は回帰テスト→GitHub Pages→iPhone実機Before/After比較を行う

## 2026-08-23 — APP-002 回数表示削除のPM判断反映

### PM判断

- PRJ-003共通仕様の「子ども画面にスコア・回数を原則表示しない」へ統一する
- APP-006「カラフル花火」と同じ設計思想にする
- 達成数ではなく、タップした瞬間の反応そのものを楽しむ構成とする

### 変更

- 子ども画面の「○こ われたよ！」を削除
- 回数表示専用の内部カウント状態と表示更新処理を削除
- 回数表示用フッターと余白を削除し、遊び領域を画面下部のsafe areaまで拡張
- 「ぽん！」「ぱちん！」、光粒、リング、効果音、初回案内は維持
- APP-002 READMEと引き継ぎ資料を現行仕様へ更新

### 状態

- 公開前の技術検証とPM仕様判断を反映済み
- APP-002をスマートフォン実機確認用の公開候補とする
- 正式公開判定は実機の複数指、音量・刺激、safe area、reduced-motion、発熱確認後に行う

## 2026-08-23 — APP-002 公開前コード検証

### 修正

- 音OFF設定を`localStorage`へ保存し、保存不可時のフォールバックを追加
- 効果音の同時再生を最大4音へ制限
- 終了したOscillator、Gain、Filterの追跡・切断処理を追加
- 音OFF、タブ非表示、途中退出時に再生中の効果音を停止
- `visibilitychange`、`pagehide`、`pageshow`へ対応し、BFCache復帰時に生成を1つだけ再開
- 非表示・退出時に生成タイマー、削除タイマー、泡、光粒、文字DOMを消去
- キーボードフォーカス中の泡を一時停止し、画面外へ消える問題を修正
- フォーカス中の泡を割った場合、遊び領域へフォーカスを戻す処理を追加

### 検証

- 390×844px縦画面、844×390px横画面、1024×768pxタブレット相当で縦横スクロールなし
- 泡72〜130px、音ボタン66〜72px、最大泡数10を確認
- 同じ泡への5回同時相当操作が1回だけ計上されることを確認
- 別々の2泡への同時相当操作が2回計上されることを確認
- 音OFFと再読み込み後の設定維持を確認
- 音あり高速操作後に光粒・文字DOMが残らないことを確認
- BFCache相当の途中退出・復帰後に泡生成と音設定が正常なことを確認
- 60秒超の連続動作後も泡が上限内で循環し、一時DOMが残らないことを確認
- Enterキー操作、フォーカス中の泡停止、破裂後のフォーカス復帰を確認
- JavaScript構文、コンソールエラー・警告なし

### 判定・保留

- コード上の技術検証はPASS
- 実際の複数指、音量・音質、safe area、reduced-motion、刺激、発熱は実機確認待ち
- 子ども画面の回数表示はPRJ-003共通仕様と衝突するため、削除せずPM判断待ち
- PM判断が終わるまで正式公開判定は保留

## 2026-08-23 — GitHub正式運用への初回移行

### 変更

- 公開Repository `yuy080622-source/toddler-web-apps`を新規作成
- PRJ-003現行7アプリを`apps/`配下へコピー移行
- 旧`Fashion`フォルダ内の動物タッチ旧版は移行対象外とした
- `AGENTS.md`を追加し、PM・ChatGPT・Codexの役割と更新ルールを明文化
- 共通のプロジェクト背景、仕様、設計、引き継ぎ、変更履歴を`docs/`へ追加
- GitHub PagesのJekyll処理を無効化する`.nojekyll`を追加
- `.DS_Store`等を除外する`.gitignore`を追加

### 移行元の扱い

- デスクトップ上の元フォルダは削除・変更せず、当面バックアップとして保持

### APP-006

- 公開前検証PASS版を`apps/APP-006-colorful-fireworks/`へ移行
- GitHub Pagesのサブディレクトリで動作する相対パス構成を維持

### 検証・公開

- 7アプリの移行元と移行先にファイル差分がないことを確認
- 全アプリのJavaScript構文を確認
- HTML内にGitHub Pagesで壊れるルート絶対パスがないことを確認
- `main`ブランチへ初回commitをpush
- GitHub Pagesを`main`／`/ (root)`から有効化
- APP-006を次のURLで公開
  - `https://yuy080622-source.github.io/toddler-web-apps/apps/APP-006-colorful-fireworks/`
- 公開URLで以下を確認
  - HTML表示：PASS
  - CSS読込：PASS
  - JavaScript初期化：PASS
  - タッチ相当操作による花火：PASS
  - 音ON／OFF表示：PASS
  - 音OFFの再読み込み後保持：PASS
  - 相対パス：PASS
  - コンソールエラー・警告：なし

## 2026-08-23 — APP-006 実機確認・正式公開判定

### 実機確認

- iPhone実機で音量・音質を確認
- 実際の複数指操作を確認
- 高速タップ時の操作感を確認
- 表示・タップ感を確認
- 光量・刺激を確認
- ユーザー確認で問題なし

### 判定

- Codex公開前検証PASSと実機確認PASSを合わせ、APP-006「カラフル花火」をMVP正式公開済みとする
- 公開後は重大不具合がない限り「効果判定待ち」とし、短期間での追加改善は行わない
- 発信素材として素材ログ `MAT-20260823-016` に記録

### 次工程

- 次の公開候補としてAPP-002「シャボン玉タッチ」の公開前検証へ進む
