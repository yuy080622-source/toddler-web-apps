# PRJ-003 引き継ぎ

更新日：2026-08-23

## 現在状態

| ID | アプリ | 状態 | 次の確認 |
|---|---|---|---|
| APP-002 | シャボン玉タッチ | **MVP正式公開済み・実機確認PASS** | 効果判定待ち |
| APP-003 | くだものポン！ | **コード公開前検証PASS・公開候補** | 「まえ／つぎ」のPM判断、実機の複数指・音量・刺激 |
| APP-004 | 音あそびピアノ | Repository移行済み。主要実装あり | 複数指、指滑り、同時発音、音量 |
| APP-005 | できたよ！生活習慣 | Repository移行済み。主要実装あり | 3モード完走、ドラッグ、途中退出 |
| APP-006 | カラフル花火 | **MVP正式公開済み・打ち上げ演出改善仕様確定／実装待ち** | Codex実装→Before/After実機比較 |
| APP-007 | どうぶつをタッチ！ | Repository移行済み。保護者機能あり | タイマー、統計、長押し、実機音声 |
| APP-008 | いないいないばあ！ | Repository移行済み。主要実装あり | カーテン速度、読み上げ、連打 |

## APP-002 正式公開完了

2026-08-23、Codex公開前検証、PM仕様判断反映、iPhone実機確認を完了し、APP-002「シャボン玉タッチ」をMVP正式公開済みとした。

### 実装・仕様

- 子ども画面の回数表示と内部カウント処理を削除
- 「ぽん！」「ぱちん！」、光粒、リング、効果音、初回案内は維持
- 音OFF設定を端末内保存し、保存失敗時もアプリ継続
- 同時効果音最大4音
- AudioNode解放
- `visibilitychange`、`pagehide`、`pageshow`、BFCache対応
- 非表示・退出時に泡、光粒、文字DOM、タイマー、音声を停止・削除
- フォーカス中の泡を停止し、キーボード利用時の画面外消失を防止
- 390×844、844×390、1024×768で縦横スクロールなし
- 60秒超の連続動作でDOM残留・増加なし

### iPhone実機確認

以下をユーザー実機で確認し、すべて問題なし。

- 通常操作
- 2本指で別々の泡を同時に割る操作
- 音OFFと、ページを閉じて再度開いた後の音OFF保持
- 縦→横→縦の画面回転
- バックグラウンド移行後の復帰
- 高速連打時の音量・刺激・表示
- 「視差効果を減らす」有効時に泡の動きがゆっくりになること
- 1〜2分程度の継続利用で不自然な発熱・動作低下がないこと

### 判定

- コード検証：PASS
- PM仕様判断：反映済み
- iPhone実機確認：PASS
- 正式公開判定：PASS
- 現在状態：**効果判定待ち**

重大不具合がない限り、公開直後に効果を断定せず、PRJ-003共通ルールに従って観察する。

## APP-003 公開前検証

2026-08-23、コード上の公開前検証と公開阻害問題の修正を完了し、スマートフォン実機確認用の公開候補とした。

### 修正・確認

- `visibilitychange`、`pagehide`、`pageshow`、BFCache復帰へ対応
- 非表示・途中退出時に演出タイマー、粒子、名前表示、音声を停止・削除
- 古いMP3失敗コールバックが復帰後に読み上げを開始しないよう実行IDと表示状態で制御
- MP3未配置を記録し、確認後のタップはWeb Speech APIへ直接フォールバック
- reduced-motion時の粒子を8個から4個へ減らし、移動距離も短縮
- safe areaの内側へ通常余白を加え、音ボタンを64px以上へ拡大
- 4果物の絵文字フォールバック、固有演出、切替、古い演出除去を確認
- 高速100回操作後も粒子、文字、タイマー用DOMが残らず、コンソールエラー・警告なし
- 390×844、844×390、1024×768で縦横スクロールなし
- 音OFFと再読み込み・BFCache復帰後の設定維持、キーボード・ARIAを確認
- 子ども画面に回数・スコア表示なし

### PM判断待ち

「まえ」「つぎ」は大きなタップ対象で4種類を確実に循環できる一方、1歳前後の子どもには果物以外の操作対象・誤操作要因になり得る。削除やナビゲーション方式変更は行わず、現行方式の採否をPM判断待ちとする。

### 実機確認待ち

実際の複数指、Web Speech APIの日本語音声・音量、safe area、reduced-motion、幼児本人の操作感・誤操作、長時間利用時の発熱。

## APP-006 公開完了

Codex公開前検証とiPhone実機確認はPASS済み。既存MVPは正式公開済み。

- 子ども画面から回数表示を削除
- 音OFF設定を端末内保存
- 保存失敗時のフォールバック
- 同時効果音最大4音
- AudioNode解放
- `visibilitychange`対応
- `pagehide`／`pageshow`／BFCache対応
- 高速260回操作PASS
- 粒子上限200
- 1024×768表示確認
- JavaScript・コンソールエラーなし
- iPhone実機で音量・操作感・複数指・高速タップ・表示・刺激を確認し、ユーザー確認ですべて問題なし

## APP-006 改善：打ち上げ演出

2026-08-23、実利用・レビューから「タップ位置で即座に開くより、画面下から打ち上がってから開く方が打ち上げ花火の全体像が分かりやすい」という改善案を採用した。

### PM確定仕様

- 従来：タップ位置で直接花火が開く
- 改善後：画面下から小さな光が打ち上がり、タップ位置付近で花火が開く
- 地面・街並みは追加しない
- X座標はタップ位置を基本維持する
- Y座標はタップ位置を基本とし、上端・下端で安全範囲へ補正する
- 上端は画面上端から約10%より下を目安
- 下端は画面高さ約70%より上を最低爆発高度の目安
- 画面下部タップ時もX位置は維持し、Yのみ最低高度へ補正する
- タップ直後に上昇反応を開始し、無反応時間を作らない
- 通常の打ち上げ時間は約0.3〜0.5秒を目安
- 打ち上げ表現は小さな光の玉＋短い尾
- 上昇音は追加せず、既存の花火効果音のみ
- 既存の最大粒子200、同時効果音4、音OFF保存、中断・復帰、安全処理を維持
- `prefers-reduced-motion`では打ち上げ時間・移動・尾を短縮し、既存の粒子数・寿命・発光低減も維持
- 高速タップ・複数指で打ち上げオブジェクトやタイマーが増え続けないよう確実に破棄する

### Before素材

改善前のiPhone縦画面録画を2026-08-23に取得済み。約10.8秒。旧仕様「タップ位置で直接花火が開く」状態を記録している。Drive保存済みで素材ログ`MAT-20260823-016`に紐付け済み。

Afterは新仕様実装後、同程度の長さで上部・中央・下部タップを含む操作を撮影して比較する。

### 次の工程

1. CodexがAPP-006の打ち上げ演出を確定仕様どおり実装する。
2. 既存機能・安全処理の回帰テストを行う。
3. GitHub Pagesへ反映する。
4. iPhone実機で上部・中央・下部タップ、複数指、高速タップ、音量、刺激、発熱、reduced-motionを確認する。
5. After動画を取得しBeforeと比較する。
6. 改善結果を「効果判定待ち」とし、重大不具合がない限り短期で追加変更しない。

## 公開

- Repository：`https://github.com/yuy080622-source/toddler-web-apps`
- branch：`main`
- GitHub Pages：mainブランチの`/ (root)`から公開済み
- APP-002公開URL：`https://yuy080622-source.github.io/toddler-web-apps/apps/APP-002-bubble-touch/`
- APP-006公開URL：`https://yuy080622-source.github.io/toddler-web-apps/apps/APP-006-colorful-fireworks/`

## 次の作業

1. APP-006の打ち上げ演出をCodexで実装・検証する。
2. APP-006の実機Before/After比較を完了する。
3. APP-003の「まえ／つぎ」の扱いをPM判断する。
4. GitHub Pages上でAPP-003の複数指、音声・音量、safe area、reduced-motion、幼児の操作感、発熱を実機確認する。
