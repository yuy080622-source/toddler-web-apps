# くだものポン！

1歳前後の子どもと保護者が、果物へのタッチとやさしい反応を短時間楽しむためのWebアプリです。正解・不正解やスコアはありません。

## 起動方法

`index.html` をブラウザで開くだけでも動作します。音声素材やブラウザ読み上げを安定して使うには、`fruit-pop` フォルダでローカルサーバーを起動してください。

```sh
python3 -m http.server 8000
```

その後、ブラウザで `http://localhost:8000` を開きます。

## 構成

- `index.html`：画面構造
- `style.css`：レイアウト、配色、4種の演出、スマートフォン対応
- `script.js`：果物データ、自動切り替え、音声、連打制御、素材フォールバック
- `assets/images/`：任意の果物画像
- `assets/sounds/`：将来導入する正式な名前音声

外部ライブラリ、広告、通信、アクセス解析は使用していません。音声設定だけを端末の `localStorage` に保存します。

## 素材の差し替え

次の名前で素材を配置すると、自動で絵文字・読み上げから素材へ切り替わります。

| 果物 | 画像 | 音声 |
|---|---|---|
| りんご | `assets/images/apple.webp` | `assets/sounds/apple.mp3` |
| バナナ | `assets/images/banana.webp` | `assets/sounds/banana.mp3` |
| いちご | `assets/images/strawberry.webp` | `assets/sounds/strawberry.mp3` |
| ぶどう | `assets/images/grape.webp` | `assets/sounds/grape.mp3` |

- 画像は正方形に近く、背景が透明なWebPを推奨します。
- 音声は短く、音量をそろえたMP3を推奨します。
- 現在のMVPは正式MP3を配置していないため、果物タップの処理内でWeb Speech APIの日本語読み上げを直接開始します。存在しないMP3へのアクセスや、読込失敗待ちは行いません。
- 正式MP3を導入するときは、素材配置後に`script.js`の`useOfficialAudio`を`true`へ変更するとMP3優先・失敗時読み上げへ切り替わります。
- 画像がない場合は絵文字を使います。画像・音声APIが使えなくても視覚演出は動きます。
- 別のファイル名を使う場合は、`script.js` 冒頭の `fruits` 配列を変更してください。

## 確認項目

- 起動時にりんごが表示される
- 果物を触ると名前、固有の動き、8個の装飾が約700ms表示される
- 演出後に、りんご→バナナ→いちご→ぶどうの順で1つだけ自動的に進む
- 子ども画面の操作対象は中央の果物だけで、「まえ」「つぎ」ボタンやページ表示はない
- 素早く連打・複数指操作しても同じ果物の演出が再開始され、最後の入力から1回だけ次へ進む
- 切り替え時に途中の音声と古い演出を停止する
- 音声ボタンでオン・オフでき、再読み込み後も設定が保持される
- タップ直後にWeb Speech APIを呼び出し、約720msの果物切り替えでは読み上げを強制停止しない
- 高速タップ時は直前の読み上げを停止して再開始し、音声を重ねない
- 素材なしで絵文字とWeb Speech APIが使われる
- タブ非表示、途中退出、BFCache復帰時に古い演出・音声・タイマーが残らない
- 390×844、844×390、1024×768で縦横スクロールせず操作できる
- 「視差効果を減らす」等の設定時に動き、粒子数、移動距離が弱くなる

## 公開前検証

2026年8月23日に、旧「まえ／つぎ」方式で公開前検証を実施しました。2026年8月24日にPM確定仕様へ合わせてナビゲーションを削除し、果物タップ→演出→次の果物へ自動循環する方式へ変更しました。

iPhone Safari実機では、旧MP3失敗待ち方式で音声が出ずFAILでした。今回の直接読み上げ方式をGitHub Pagesへ反映後、まず音が出るかを再確認し、その後に声質・音量を判定します。

現在の公開前検証結果と実機確認待ち項目は、Repositoryの`docs/handoff.md`を参照してください。

## 補足

ブラウザは、音声再生をユーザー操作後にだけ許可する場合があります。本アプリの読み上げは果物を触ったときに開始するため、その制限に沿っています。
